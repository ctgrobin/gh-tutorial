name: 4-Multi Jobs Workflow

on:
  workflow_dispatch:

jobs:
  workflow-job-one:
    runs-on: ubuntu-latest

    steps:
      - name: Job One Step One
        run: |
          echo "Hello, First Workflow step one"
    
      - name: Job One Step two
        run: |
          echo "Hello, First Workflow step two"      


  workflow-job-two:
    runs-on: ubuntu-latest

    steps:
      - name: Job two step one
        run: |
          echo "Hello, Second Workflow! step one"

      - name: Job two step two
        run: |
          echo "Hello, Second Workflow! step two"    