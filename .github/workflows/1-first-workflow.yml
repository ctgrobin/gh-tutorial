name: 1-First Workflow

on:
  push:
    branches:
      - main

jobs:
  first-workflow-job:
    runs-on: ubuntu-latest

    steps:
      - name: First step
        run: |
          echo "Hello, World!"
          echo "Hello Again!"